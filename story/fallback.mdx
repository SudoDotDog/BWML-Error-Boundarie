---
name: Fallback
---

import { FallbackErrorBoundary } from "../src/index";
import { MockBugComponent } from "./mock"
import { Playground } from 'docz'

# Fallback Error Boundary

## Fallback React Node

<Playground>
  <FallbackErrorBoundary
    fallback={<div>Caught!</div>}
  >
    <MockBugComponent />
  </FallbackErrorBoundary>
</Playground>

## Fallback React Component

<Playground>
  <FallbackErrorBoundary
    fallbackComponent={() => <div>Caught!</div>}
  >
    <MockBugComponent />
  </FallbackErrorBoundary>
</Playground>
