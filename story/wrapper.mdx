---
name: Wrapper Error Boundary
---

import { WrapperErrorBoundary } from "../src/index";
import { SimpleMockBugComponent } from "./mock/simple";
import { Playground } from "docz";

# Wrapper Error Boundary

## Fallback React Node

<Playground>
  <WrapperErrorBoundary
    fallback={<div>Caught!</div>}
  >
    <SimpleMockBugComponent />
  </WrapperErrorBoundary>
</Playground>

## Fallback React Component

<Playground>
  <WrapperErrorBoundary
    fallbackComponent={() => <div>Caught!</div>}
  >
    <SimpleMockBugComponent />
  </WrapperErrorBoundary>
</Playground>

## Empty Children

<Playground>
  <WrapperErrorBoundary
    fallbackComponent={() => <div>Caught!</div>}
  />
</Playground>
