---
name: Function Error Boundary
---

import { FunctionErrorBoundary } from "../src/index";
import { TriggerableMockBugComponent } from "./mock/triggerable";
import { Playground } from "docz";

# Function Error Boundary

## Fallback React Node

<Playground>
  <FunctionErrorBoundary
    fallback={<div>Caught!</div>}
  >
    {(props) => <TriggerableMockBugComponent {...props} />}
  </FunctionErrorBoundary>
</Playground>

## Fallback React Component

<Playground>
  <FunctionErrorBoundary
    fallbackComponent={() => <div>Caught!</div>}
  >
    {(props) => <TriggerableMockBugComponent {...props} />}
  </FunctionErrorBoundary>
</Playground>

## Empty Children

<Playground>
  <FunctionErrorBoundary
    fallbackComponent={() => <div>Caught!</div>}
  />
</Playground>
